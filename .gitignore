# Editor directories and files
.vscode/
.idea/
.cursor/
.cursorrules/
.claude/
CLAUDE.md
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Temporary files
*.tmp
*.temp

# Build files (if you add a build process later)
dist/
build/

# Node modules (if you add npm later)
node_modules/

# Package files
*.crx
*.pem
*.zip

# Rust/WASM build artifacts
# Note: src/resize.wasm is pre-compiled and should be committed
src/wasm-resize/target/
src/wasm-resize/Cargo.lock
src/wasm-resize/**/*.rs.bk
src/wasm-resize/.cargo/.cargo-ok

# Rust compilation artifacts
**/*.rlib
**/*.rmeta
**/*.d
**/*.pdb

# WASM build artifacts (except pre-compiled resize.wasm)
# Keep: src/resize.wasm (pre-compiled for end users)
# Ignore: target directory and other build outputs
src/wasm-resize/target/**/*.wasm
!src/resize.wasm